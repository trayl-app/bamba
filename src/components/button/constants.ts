export const VARIANT = Object.freeze({
  primary: 'primary',
  secondary: 'secondary',
  tertiary: 'tertiary',
});

export const SIZE = Object.freeze({
  mini: 'mini',
  default: 'default',
  compact: 'compact',
  large: 'large',
});

export const SHAPE = Object.freeze({
  default: 'default',
  pill: 'pill',
});
